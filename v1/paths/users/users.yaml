get:
  summary: List all users
  description: This endpoint will retrieve the details of all Fire users on your acount.
  tags:
    - Profile Management
  operationId: getUsers
  responses:
      '200':
        description: List of all Users.
        content:
          application/json:
            schema:
              type: array
              items: 
                $ref: '#/components/schemas/User'