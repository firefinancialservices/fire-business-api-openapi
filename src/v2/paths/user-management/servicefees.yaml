get:
  summary: Get the services and fees associated with them on your fire.com account
  description: Returns an array of the services and fees associated with them
  tags:
   - Users
  operationId: getServices
  responses:
    '200':
          description: An object of services information
          content:
            application/json:
              schema:
                $ref: '../../schemas/businessServices-array.yaml'
    '401':
          $ref: '../../responses/unauthorised-error.yaml'


