get:
  summary: List Debit Cards
  description: Returns a list of cards related to your fire.com account.
  tags:
    - Debit Cards
  operationId: getListofCards
  responses: 
    '200':  
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../schemas/cards.yaml' 
    '403':
      $ref: ../../responses/unauthorised-error.yaml
    '401':
      $ref: ../../responses/unauthorised-error.yaml


post:
  summary: Create a new Fire debit card
  description: You can create multiple debit cards which can be linked to your fire.com accounts.
  tags:
    - Debit Cards
  operationId: createNewCard
  requestBody:
    required: true
    description: Details of the new card
    content:
      application/json:
        schema:
          $ref: '../../../fire-business-api-v1.yaml#/components/schemas/NewCard'
  responses:
    '200':
      description: Card created successfully
      content:
        application/json:
          schema:
            $ref: '../../../fire-business-api-v1.yaml#/components/schemas/NewCardResponse'
